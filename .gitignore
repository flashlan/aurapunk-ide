# Rust
target/
/target
**/*.rs.bk

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Build outputs
/dist
/build
bindings/

# Environment variables
.env
.env.remote
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output

# ESLint cache
.eslintcache

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Storybook build outputs
.out
.storybook-out

.env
packages/**/dist

# TypeScript incremental build cache
*.tsbuildinfo

build-npm-package-codesign.sh

npx-cli/bin/
npx-cli/dist
npx-cli/vibe-kanban-*
vibe-kanban-*.tgz

# Platform-specific companion executables copied into desktop installers by CI.
crates/tauri-app/resources/bin/*
!crates/tauri-app/resources/bin/.gitkeep
crates/tauri-app/resources/plugins/*
!crates/tauri-app/resources/plugins/.gitkeep

# Development ports file
.dev-ports.json

dev_assets
/packages/web/.env.sentry-build-plugin
.ssh

vibe-kanban-cloud/

.jj
.sisyphus/

# Vite temp files
*.timestamp-*.mjs

# Mobile testing (per-developer, do not commit)
*.ts.net.crt
*.ts.net.key
Caddyfile
Caddyfile.dev

# Claude Code local session state (locks, scheduled tasks, settings) - never commit
.claude/

IMPLEMENTATION_PLAN.md
SPEC.md

# Local mem0 volume backups (may contain private project memory)
mem0-vk/backups/

# Local runtime data / build artifacts
db.v2.sqlite
installer-output/
